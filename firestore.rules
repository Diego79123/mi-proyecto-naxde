
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Acceso público total para permitir el despliegue y pruebas sin restricciones.
      allow read, write: if true;
    }
  }
}
