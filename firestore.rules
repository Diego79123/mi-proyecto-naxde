
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // ACCESO PÚBLICO TOTAL para facilitar el despliegue y pruebas.
      allow read, write: if true;
    }
  }
}
